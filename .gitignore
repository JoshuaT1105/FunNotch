# Build products — everything here is regenerated by ./build.sh
build/
dist/
*.app
*.icns

# Release binaries live on GitHub Releases, not in the repo. Keeping a 2.8 MB
# dmg in git history is permanent and grows with every version.
*.dmg
*.zip

# Swift
.build/
.swiftpm/
Package.resolved

# macOS
.DS_Store
._*
.Spotlight-V100
.Trashes

# Editors
.vscode/
.idea/
*.xcuserstate
xcuserdata/

# Local scratch
*.log
.env
